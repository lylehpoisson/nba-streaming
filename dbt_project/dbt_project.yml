name: nba_pipeline
version: "1.0.0"
config-version: 2

profile: nba_pipeline

model-paths: ["models"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nba_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts